# Claude Code lessons

Ordered outline - a session works through the next lesson you haven't
completed. See "Related skills" in the README before your first one.
Personal progress lives in gitignored `progress/`, not here.

1. This repo - how sessions run, the `dojo`/`dojo-creation` skills,
   the `lessons.md`/`progress/`/`notes/`/`sandboxes/` model, and the
   Claude Code basics (skills, `CLAUDE.md`, slash commands) needed to
   use this repo.
